[package]
name = "youth-invalid-sequence"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Youth containment fixture that claims to process an unsent event."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51.0"
