[package]
name = "youth-invalid-revision"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Youth containment fixture that returns an impossible revision transition."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51.0"
