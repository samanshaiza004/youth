[package]
name = "youth-trap-on-handle"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Youth containment fixture that traps while handling an event."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51.0"
