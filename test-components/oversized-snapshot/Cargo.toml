[package]
name = "youth-oversized-snapshot"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Youth containment fixture that returns an oversized text value."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51.0"
