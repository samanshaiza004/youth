[package]
name = "youth-memory-bomb"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Youth containment fixture that grows linear memory without bound."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51.0"
