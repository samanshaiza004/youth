[package]
name = "youth-invalid-patch"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Youth containment fixture that returns an invalid patch batch."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51.0"
