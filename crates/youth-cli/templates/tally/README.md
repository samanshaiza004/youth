# {{display_name}}

A Youth application generated from the Developer Preview 0 Tally template.

```text
youth check
youth test
youth dev
youth build --release
```

The vendored WIT directory is an inspectable protocol snapshot. Rust bindings
and export plumbing come only from the exact `youth-sdk` revision in
`Youth.lock`.
